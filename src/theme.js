import { createTheme } from "@mui/material"

export const theme = createTheme({
    palette:{
        primary:{
            main: '#FFFFFF',
            light: '#FCFCFC'
        },
        secondary:{
            main: 'linear-gradient(112.92deg, #5404FF 18.35%, rgba(189, 59, 210, 0.94) 71.49%)'
        }
    }
})